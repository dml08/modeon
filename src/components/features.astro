---
import { Icon } from "astro-icon/components";
import ContentSection from "~/components/content-section.astro";
import type { FeatureItem } from "~/types";
import {Image} from "astro:assets";

const features: Array<FeatureItem> = [
  {
    title: "5 músicos en escena",
    description:
      "100% en vivo, con una amplia trayectoria en la música y la animación de eventos.",
    icon: "keyboard",
  },
  {
    title: "Equipo técnico",
    description:
      "Contamos con sonido de ultima generación y equipo especializado.",
    icon: "sound",
  },
  {
    title: "60 minutos de SHOW",
    description:
        "Ofrecemos un espectáculo de una hora lleno de ritmos tropicales, tanto clásicos como modernos",
    icon: "clock",
  },
  {
    title: "Fiesta",
    description:
      "Nos encargamos de que tu evento sea inolvidable, con la mejor música y la mejor onda.",
    icon: "party",
  },
];
---

<section id="features" class="p-6">
  <h1 class="gradient-text text-center font-extrabold tracking-tight text-6xl">
    Mode On
  </h1>
  <p class="py-6 text-center">
    Somos una banda de cumbia formada en 2013 en San Antonio de Padua, Buenos Aires. Con 10 años de trayectoria llevando alegría y diversión a cada evento, nos especializamos en reinventar éxitos de diversos géneros con un estilo único, haciendo vibrar a todos con nuestra energía en vivo.
  </p>
  <ul class="flex flex-wrap justify-center align-middle">
    {
      features.map(({ title, description, icon }) => (
        <li class="flex flex-col items-center gap-4 border border-default bg-offset p-6 lg:flex-1 max-w-64">
          <div class="size-16 rounded-full border-2 border-current p-3">
            <Icon name={icon} class="size-full" />
          </div>
          <p class="text-center font-extrabold text-xl">{title}</p>
          <p class="text-center text-offset text-sm">{description}</p>
        </li>)
      )
    }
  </ul>
</section>
